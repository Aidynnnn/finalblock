// Superseded by the top-level Analytics component which uses the named
// queries from subgraph/queries.graphql via the typed GraphQL client.
export { Analytics as AnalyticsPanel } from '../Analytics'
